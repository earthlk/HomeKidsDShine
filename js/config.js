// ============================================================
// Little Stars — config.js
// ค่าตั้งต้นฝั่งหน้าเว็บ แก้ที่นี่ที่เดียว
// ============================================================

const CONFIG = {
  // ⚠️ หลัง Deploy > New deployment > Web app ใน Apps Script แล้ว
  //    เอา URL ที่ลงท้ายด้วย /exec มาใส่ตรงนี้
  GAS_URL: 'ใส่_URL_ของ_GAS_Web_App_ที่นี่',

  // ที่อยู่ของหน้าเว็บนี้ ใช้ตอนเปิด OAuth ใน Phase หลัง
  SITE_URL: 'https://earthlk.github.io/HomeyKids/',

  // คีย์เก็บข้อมูลใน sessionStorage
  STORE_KEY: 'ls.session',
};
